'use client';
import {useState} from 'react';
import {ArrowRight} from 'lucide-react';
import {Dialog,DialogContent,DialogTitle,DialogDescription} from '@/components/ui/dialog';
import {categories,catalog} from './catalog';
export function ProductCatalog(){const [selected,setSelected]=useState<(typeof catalog)[number]|null>(null);return <><nav className="category-links" aria-label="Product categories">{categories.map((category,index)=><a key={category} href={`#category-${index}`}>{category}</a>)}</nav>{categories.map((category,index)=><section className="catalog-category" id={`category-${index}`} key={category}><div className="catalog-category-heading"><h2>{category}</h2><span>2 sample products</span></div><div className="catalog-grid">{catalog.filter(item=>item.category===category).map(item=><button className="catalog-card" key={item.id} onClick={()=>setSelected(item)}><div className="catalog-placeholder" style={{background:item.color}}><span>CONCORD</span><span className="placeholder-index">0{item.variant}</span><small>Image placeholder</small></div><div className="catalog-card-copy"><span className="sample-badge">Sample product</span><h3>{item.name}</h3><p>{item.format} · Example pack size</p><span className="catalog-card-action">Product details <ArrowRight size={20}/></span></div></button>)}</div></section>)}<Dialog open={!!selected} onOpenChange={open=>{if(!open)setSelected(null)}}><DialogContent className="detail-dialog"><div className="detail-color" style={{background:selected?.color}}>Image placeholder</div><DialogTitle className="detail-title">{selected?.name}</DialogTitle><DialogDescription className="detail-description">{selected?.description}</DialogDescription><dl className="product-specs"><div><dt>Category</dt><dd>{selected?.category}</dd></div><div><dt>Example pack size</dt><dd>{selected?.format}</dd></div></dl><a className="pill" href="/contact">Enquire about this product <ArrowRight size={20}/></a></DialogContent></Dialog></>}
